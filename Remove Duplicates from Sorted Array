class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if(nums.empty())return 0;  
        int num=1,i;  
        for(i=1;i<nums.size();++i)  
            if(nums[i]!=nums[i-1])  
                nums[num++]=nums[i];    
        return num;  
    }
};
